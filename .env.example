# Server Configuration
NODE_ENV=development
PORT=3001

# Geneline-X Configuration
GENELINE_HOST=https://message.geneline-x.net
GENELINE_API_KEY=your-api-key-here
GENELINE_CHATBOT_ID=your-chatbot-id-here

# Optional: System prompt to restrict chatbot responses to specific topics
# Default: Restricts to public health topics in Sierra Leone
# GENELINE_SYSTEM_PROMPT="You are a public health assistant for Sierra Leone. You ONLY answer questions related to public health topics in Sierra Leone, including but not limited to: diseases (malaria, cholera, Ebola, etc.), vaccinations, healthcare facilities, maternal and child health, nutrition, sanitation, hygiene, disease prevention, and health services. If a user asks about topics unrelated to public health in Sierra Leone, politely explain that you can only assist with public health questions about Sierra Leone and encourage them to ask a relevant question."

# WhatsApp Configuration
WHATSAPP_CLIENT_ID=main-bot
ALLOW_GROUP_MESSAGES=false

# Admin API
ADMIN_API_KEY=your-admin-secret-here

# Queue & Rate Limiting
MAX_CONCURRENCY=5
PER_CHAT_RATE_LIMIT_MS=1000

# Document Ingestion
# Optional: Separate host for ingestion API (defaults to GENELINE_HOST if not set)
# GENELINE_INGEST_HOST=https://ingest.geneline-x.net
INGEST_POLL_INTERVAL_MS=5000
MAX_FILE_SIZE_MB=50
ALLOWED_FILE_TYPES=application/pdf

# Agent Mode (Custom Agent Framework on Geneline-X)
# Enable agent mode for more interactive, context-aware conversations
ENABLE_AGENT_MODE=false
# Maximum tool calls per conversation (prevents infinite loops)
AGENT_MAX_ITERATIONS=5
# Number of messages to keep in conversation history
AGENT_CONVERSATION_HISTORY_LIMIT=10

# Supabase Configuration (for Admin Dashboard integration)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
